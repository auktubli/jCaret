# jCaret

محرر نصوص غني (Rich Text Editor) خفيف الوزن، متكامل الميزات، ولا يعتمد على أي مكتبات خارجية. يعرض jCaret المستندات على صفحات A4 حقيقية — بهوامش مستقلة لكل صفحة، وتقسيم تلقائي للصفحات، وترقيم للصفحات، وملف PDF قابل للطباعة يطابق ما يظهر على الشاشة تمامًا — ويدعم اللغات من اليسار إلى اليمين ومن اليمين إلى اليسار مباشرة دون أي إعدادات إضافية.

صُنع بحب من قِبل **[auktubli.com](https://www.auktubli.com)**.

---

## أبرز الميزات

- **تخطيط صفحات A4** — يتدفق النص تلقائيًا عبر الصفحات بالشكل الذي سيُطبع عليه فعليًا، مع إرشادات مرئية للصفحات على الشاشة.
- **هوامش مستقلة لكل صفحة** — يمكن لكل صفحة أن تملك هوامشها العلوية والسفلية واليسرى واليمنى الخاصة بها، وتُعدَّل من لوحة عائمة.
- **طباعة كملف PDF** — مسار طباعة مخصص يطابق تخطيط الصفحة على الشاشة تمامًا، مع الحفاظ على الروابط قابلة للنقر.
- **تنزيل أي صفحة كصورة PNG** — تصدير صفحة واحدة كصورة عالية الجودة دون أي مكتبة خارجية.
- **علامة مائية** — علامة مائية واحدة قطرية شبه شفافة تُطبَّق على كل صفحة (الحالية والمستقبلية)، وتُحفظ بين الجلسات.
- **الكتابة اليدوية / الرسم** — الرسم مباشرة على الصفحة بقلم ومُظلِّل (Highlighter)، مع إمكانية المسح، وإعادة فتح أي رسمة لاحقًا لمواصلة التعديل عليها — فهي لا تتحول أبدًا إلى صورة ثابتة.
- **صور قابلة للسحب وتغيير الحجم والتدوير** — تطفو الصور والرسومات بحرية على الصفحة: اسحبها إلى أي مكان، غيّر حجمها، أو دوّرها، وهي تبقى بذكاء داخل المنطقة القابلة للطباعة.
- **تنسيق نصوص غني** — غامق، مائل، تسطير، يتوسطه خط، أعلى/أسفل (Superscript/Subscript)، حجم الخط، عائلة الخط، لون الخط، ألوان التمييز، الاقتباسات، القوائم المرتبة وغير المرتبة، المحاذاة، وإزالة التنسيق.
- **كتل الكود (Code Blocks)** — حوّل أي نص محدد إلى كتلة كود مُلوَّنة بحسب الصياغة، أو كود مضمّن، مع اكتشاف تلقائي للغة البرمجة (JavaScript، Python، HTML، SQL، Shell، CSS، PHP، Go وغيرها).
- **الجداول** — إدراج جداول تصل إلى 10 أعمدة، مع إمكانية إضافة أو حذف الصفوف والأعمدة، وتصغّر الجداول تلقائيًا لتلائم هوامش الصفحة.
- **الصور والروابط** — رفع وضغط الصور تلقائيًا، إدراج روابط تشعبية، وتحويل عناوين الروابط والبريد الإلكتروني النصية إلى روابط فعلية تلقائيًا عند الطباعة.
- **قائمة إيموجي**، و**تسميات توضيحية (Captions)** للصور والجداول، ومرجع مدمج **لاختصارات لوحة المفاتيح**.
- **تراجع وإعادة كاملة** للتاريخ، وحفظ اختياري عبر **localStorage** (يشمل الهوامش، والعلامة المائية، والرسومات اليدوية — كل شيء يبقى كما هو بعد إعادة تحميل الصفحة).
- **دعم ثنائي اللغة جاهز للاستخدام** — واجهة بالعربية والإنجليزية مع اتجاه نص صحيح (RTL/LTR)، وواجهة مُعكوسة تلقائيًا، وخطوط مناسبة للعربية.

---

## البدء السريع

أضِف ملف السكربت وأنشئ عنصر حاوية (container):

```html
<div id="editor-container"></div>
<script src="jcaret.js"></script>
<script>
  const editor = new jCaret('#editor-container', {
    width: '880px',
    height: '400px',
    heightMode: 'fixed',       // 'fixed' أو 'min' (يتمدد مع المحتوى)
    useLocalStorage: true,
    language: 'ar',            // 'en' أو 'ar'
    showPageBreaks: true,      // تفعيل نظام صفحات A4
    margins: { top: 20, bottom: 20, left: 20, right: 20 }
  });
</script>
```

هذا كل ما تحتاجه — يتم إنشاء شريط الأدوات، ومنطقة الصفحة، وكل نافذة منبثقة تلقائيًا داخل الحاوية التي حددتها.

---

## خيارات الإعداد

| الخيار | القيمة الافتراضية | الوصف |
|---|---|---|
| `width` | `'880px'` | العرض الأقصى لحاوية المحرر. |
| `height` | `'400px'` | ارتفاع (أو أدنى ارتفاع) لمنطقة المحرر. |
| `heightMode` | `'fixed'` | `'fixed'` لارتفاع ثابت، أو `'min'` للسماح للمحرر بالتمدد مع محتواه. |
| `useLocalStorage` | `false` | حفظ المحتوى والهوامش والعلامة المائية عبر إعادة تحميل الصفحة. |
| `language` | `'en'` | لغة الواجهة: `'en'` أو `'ar'`. تُحدَّد اتجاهية النص تلقائيًا. |
| `borderRadius` | `'0'` | استدارة حواف حاوية المحرر. |
| `showPageBreaks` | `true` | تفعيل نظام صفحات A4 (الهوامش، ترقيم الصفحات، "إضافة صفحة"، لوحة هوامش كل صفحة). |
| `margins` | `{top:10, bottom:10, left:10, right:10}` | الهوامش الافتراضية للصفحة بالمليمتر، تُستخدم إلا إذا كانت الصفحة تملك هوامشها الخاصة. |
| `pageNumberInset` | `15` | المسافة بالمليمتر من الحافة اليمنى للصفحة إلى رقم الصفحة "1 / 3". |

---

## نظام الصفحات

عند تفعيل `showPageBreaks`، يتصرف المحرر كمعالج نصوص قائم على الصفحات:

- يتدفق المحتوى تلقائيًا إلى صفحة جديدة عندما لا يعود يتسع في الصفحة الحالية، بالضبط كما سيظهر عند الطباعة.
- يمكن لكل صفحة أن تملك **هوامشها الخاصة** — اضغط على أيقونة الهامش الصغيرة في أي صفحة، أو على زر "هوامش الصفحة" في شريط الأدوات، لفتح لوحة الهوامش العائمة. من هناك يمكنك أيضًا **تطبيق هوامش صفحة واحدة على كل الصفحات**، أو **إعادة ضبط** صفحة إلى القيم الافتراضية، أو **تنزيل تلك الصفحة كصورة PNG**.
- زر **"إضافة صفحة"** يُدرج فاصل صفحة يدويًا.
- شارة صغيرة في الزاوية تُظهر دائمًا الصفحة التي يوجد بها المؤشر حاليًا، وعدد صفحات المستند.
- **الطباعة كملف PDF** تُخرج المستند بنفس تخطيط الشاشة تمامًا — نفس الهوامش، نفس فواصل الصفحات، نفس العلامة المائية وترقيم الصفحات — وتحافظ على الروابط قابلة للنقر في الملف المحفوظ.

---

## العلامة المائية

اضغط على زر العلامة المائية في شريط الأدوات لتعيين سطر واحد من نص قطري شبه شفاف (مثل "سري" أو "مسودة"). تُطبَّق هذه العلامة على:

- كل صفحة موجودة حاليًا في المستند،
- كل صفحة تُضاف لاحقًا، تلقائيًا،
- ملف PDF المطبوع،
- وأي صفحة تُنزَّل كصورة PNG —

باستخدام نفس النص واللون والشفافية والزاوية تمامًا، بحيث لا تتعارض الأربعة مع بعضها أبدًا. تُحفظ العلامة المائية بين الجلسات عند تفعيل `useLocalStorage`.

---

## الكتابة اليدوية والرسم

اضغط على زر الكتابة اليدوية (القلم) لفتح شريط أدوات عائم صغير يحتوي على **قلم**، و**مُظلِّل (Highlighter)**، و**ممحاة**، ولوحة ألوان، وحجم قابل للتعديل، وتراجع/إعادة، وأزرار تم/إلغاء. يحدث الرسم مباشرة فوق الصفحة — وليس في مربع منفصل — بحيث يمكنك الرسم بجانب نصك مباشرة.

تتحول الرسمة المكتملة إلى شكل صغير مستقل على الصفحة يمكنك سحبه وتغيير حجمه وتدويره تمامًا كالصورة. لكن على عكس الصورة العادية، لا تتحول الرسمة أبدًا إلى صورة ثابتة: **اضغط مرتين على أي رسمة لإعادة فتحها**، وأضِف إليها ضربات فرشاة جديدة، أو امسح جزءًا منها، وتُحفظ التغييرات في تلك الرسمة نفسها.

---

## الصور والرسومات وطريقة استقرارها على الصفحة

الصور المرفوعة والرسومات اليدوية على حدٍ سواء:

- تُسحب **بحرية** إلى أي مكان على الصفحة، حتى داخل الهوامش — ولا يوقفها شيء سوى حافة الصفحة نفسها.
- تُغيَّر أحجامها باستخدام المقبض في الزاوية السفلية اليمنى؛ ويُطبَّق نفس حد حافة الصفحة بسلاسة أثناء تغيير الحجم.
- تُدوَّر باستخدام المقبض الصغير أعلى الصورة المحددة؛ ويلتقط التدوير بلطف كل 15 درجة، ويبقى أيضًا داخل حدود الصفحة.
- يمكن **محاذاتها** (يسار، وسط، يمين، أو العرض الكامل) عبر زر المحاذاة في شريط الأدوات، والذي يُثبّتها عند هامش تلك الصفحة.
- يمكن **حذفها فورًا** بمفتاح Delete أو Backspace بعد تحديدها.

---

## مرجع التنسيق

| الفئة | الأدوات |
|---|---|
| نمط النص | غامق، مائل، تسطير، يتوسطه خط، أعلى (Superscript)، أسفل (Subscript) |
| الخط | عائلة الخط، حجم الخط، لون الخط، لون التمييز |
| البنية | اقتباس، قائمة مرتبة، قائمة غير مرتبة، كتلة كود / كود مضمّن |
| المحاذاة | يسار، وسط، يمين، ضبط كامل |
| الإدراج | صورة، جدول، رابط تشعبي، إيموجي، كتابة يدوية/رسم |
| أدوات الصفحة | إضافة صفحة، هوامش الصفحة، علامة مائية، طباعة كملف PDF |
| أخرى | تراجع، إعادة، إزالة التنسيق، مسح الكل |

---

## اختصارات لوحة المفاتيح

| الاختصار | الإجراء |
|---|---|
| `Shift` + `Enter` | سطر إقتباس جديد |
| `Ctrl` + `+` | تكبير الخط على مستوى السطر |
| `Ctrl` + `-` | تصغير الخط على مستوى السطر |
| `Tab` | إدراج 4 مسافات (أو إزالة المسافة البادئة بـ `Shift` + `Tab`) |
| `Delete` | حذف صورة أو رسمة أو جدول محدد |

(هذه القائمة متوفرة أيضًا داخل المحرر عبر زر "حول" (ⓘ) في شريط الأدوات.)

---

## كتل الكود

حدّد أي نص واضغط على زر الكود لتحويله إلى كتلة كود مُلوَّنة بحسب الصياغة (أو كود مضمّن، لتحديد قصير داخل سطر واحد). تُكتشف لغة البرمجة تلقائيًا — يتم التعرف على صياغة JavaScript، وPython، وHTML، وSQL، وShell، وCSS، وPHP، وGo وغيرها — وتُلوَّن الكلمات المفتاحية، والسلاسل النصية، والتعليقات، والأرقام، وأسماء الدوال وفقًا لذلك، سواء على الشاشة أو عند الطباعة.

---

## حفظ البيانات

عند تفعيل `useLocalStorage: true`، يحفظ المحرر تلقائيًا:

- محتوى المستند،
- هوامش كل صفحة (والقيم الافتراضية المشتركة)،
- العلامة المائية،
- وكل ضربة فرشاة في كل رسمة يدوية —

بحيث تعيد إعادة تحميل الصفحة كل شيء تمامًا كما كان، بما في ذلك بكسلات أي رسمة (التي تُعاد رسمتها من بيانات الضربات المحفوظة، لأن الصورة المحفوظة وحدها لا يمكنها تخزين بكسلات عنصر Canvas).

---

## دعم المتصفحات

يُبنى jCaret على معيار `contentEditable`، وواجهة برمجة Canvas، وأحداث Pointer Events القياسية، ويعمل في أي متصفح حديث من عائلة Chromium أو Firefox أو Safari. تستخدم الطباعة إلى PDF نافذة الطباعة الأصلية للمتصفح — اختر **"حفظ بتنسيق PDF (Save as PDF)"** كوجهة للحفاظ على الروابط قابلة للنقر في الملف المحفوظ.

---

*صُنع بحب من قِبل [www.auktubli.com](https://www.auktubli.com)*
